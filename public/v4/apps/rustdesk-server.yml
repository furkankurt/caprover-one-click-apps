captainVersion: 4
services:
  hbbr:
    image: rustdesk/rustdesk-server:$$cap_version
    command: hbbr
    volumes:
      - $$cap_appname-data:/root
    ports:
      - "21117:21117"      # Relay (TCP)
      - "21119:21119"      # (optional) Web client relay (TCP)
    caproverExtra:
      notExposeAsWebApp: "true"

  hbbs:
    image: rustdesk/rustdesk-server:$$cap_version
    command: hbbs
    environment:
      - ALWAYS_USE_RELAY=$$cap_always_use_relay
    volumes:
      - $$cap_appname-data:/root
    ports:
      - "21115:21115"      # NAT type test (TCP)
      - "21116:21116"      # ID/heartbeat/connection over TCP
      - "21118:21118"      # (optional) Web client broker (TCP)
      - "21114:21114"      # (optional) Pro web console (TCP)
    depends_on:
      - hbbr

caproverOneClickApp:
  variables:
    - id: '$$cap_version'
      label: RustDesk Server Image Tag
      defaultValue: 'latest'
      description: 'Docker tag for rustdesk/rustdesk-server (e.g., latest)'
      validRegex: '/.{1,}/'

    - id: '$$cap_always_use_relay'
      label: ALWAYS_USE_RELAY
      defaultValue: 'Y'
      description: 'Y or N — Y recommended on CapRover to avoid UDP reliance'
      validRegex: '/^(Y|N|y|n)$/'

  instructions:
    start: |-
      This One-Click App deploys a **RustDesk self-hosted server**.
      Since RustDesk Server is **not an HTTP app**, it doesn’t use CapRover ingress.
      The containers listen directly on these ports (TCP only on CapRover):

        hbbs:
          - 21115/TCP — NAT type test
          - 21116/TCP — ID/heartbeat/connection (UDP not exposed on CapRover)
          - 21118/TCP — (optional) Web client support
          - 21114/TCP — (optional) Pro web console
        hbbr:
          - 21117/TCP — Relay
          - 21119/TCP — (optional) Web client support

      🔐 **Open firewall ports (copy/paste all):**
      ```bash
      sudo ufw allow 21115/tcp
      sudo ufw allow 21116/tcp
      sudo ufw allow 21117/tcp
      # Optional (enable only if you need them)
      sudo ufw allow 21118/tcp
      sudo ufw allow 21119/tcp
      sudo ufw allow 21114/tcp
      ```

      💾 Data persists in CapRover volume `$$cap_appname-data` mounted at `/root`.

      🧪 On CapRover, `ALWAYS_USE_RELAY=Y` is recommended to avoid reliance on UDP 21116 (unsupported mapping).

    end: |-
      ✅ **RustDesk Server deployment completed.**

      On the first `hbbs` start, the server generates keys and logs a line like:
      ```
      INFO [src/common.rs] Private/public key written to id_ed25519/id_ed25519.pub
      INFO [src/rendezvous_server.rs] Key: ifS35mW4JXowm5ZfYjQ1DGla2oLx8jBLB1Oj5jSTgoo=
      ```
      The public key file is located here on the host:
      ```
      /var/lib/docker/volumes/captain--$$cap_appname-data/_data/id_ed25519.pub
      ```
      Copy the contents of that file (or use the **Key:** value printed in logs).

      **Use these in RustDesk client → Settings → Network:**
      ```
      ID Server: $$cap_appname.$$cap_root_domain
      Relay Server: $$cap_appname.$$cap_root_domain
      API Server: https://$$cap_appname.$$cap_root_domain
      Key: contents of id_ed25519.pub (or the "Key:" value from logs)
      ```

  displayName: RustDesk Server
  isOfficial: false
  description: >
    Self-host your own RustDesk server. RustDesk is a full-featured open source remote control
    alternative for self-hosting and security with minimal configuration.
  documentation: 'https://github.com/rustdesk/rustdesk-server'
